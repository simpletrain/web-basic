<section class="panel panel-default">
    <div class="panel-heading">
        <h3 class="panel-title">五 、简答题</h3>
    </div>
    <div class="panel-body">
    <ol>
        <% for(var i = 0;i < allTopic.subjective.length; i++) { %>
        <li>
            <p><%= allTopic.subjective[i].question %></p>
                <textarea name=<%= allTopic.subjective[i].type + i %>
                          cols="120" rows="8"><% if(allTopic.subjective[i].input) { %><%= allTopic.subjective[i].input %><% } %></textarea>
        </li>
        <% } %>
    </ol>
    </div>
</section>